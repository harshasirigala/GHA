name: Actions
on: 
  push:
    branches:
      - main    
  pull_request:
    branches:
      - main
jobs:
  Action_demo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: print
        run: cat index.html
